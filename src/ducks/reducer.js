const initialState = {
    daily: false,
    monthly: false,
    yearly: false
}

export default function reducer(state = initialState, action) {
    switch(action.type) {
        default:
            return state
    }
}